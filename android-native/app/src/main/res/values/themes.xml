<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base theme for OpenFlix -->
    <style name="Theme.OpenFlix" parent="Theme.Leanback">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/primary</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.OpenFlix.Splash" parent="Theme.OpenFlix">
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- Player theme (immersive) -->
    <style name="Theme.OpenFlix.Player" parent="Theme.OpenFlix">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
